tags:
  - framework
  - review
category: framework
targets:
  claude: true
  codex: true
  gemini: true
# Documentary fallback path, not a runtime dependency.
portability_allow:
  - "~/.todo-db/finding-drafts/<project-id>/"
