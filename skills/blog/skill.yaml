depends:
  - SHARED/commit-framework
  - SHARED/review-protocol
  - SHARED/plan-deepening-framework
tags:
  - writing
  - blog
category: content
targets:
  claude: true
  codex: true
# Documentary optional global style-guide path, not a runtime dependency.
portability_allow:
  - "~/.claude/blog/*"
