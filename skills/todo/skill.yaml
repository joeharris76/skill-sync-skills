depends:
  - SHARED/commit-framework
  - SHARED/verify-framework
  - SHARED/research-framework
  - SHARED/slicing-framework
  - SHARED/review-protocol
  - SHARED/plan-deepening-framework
config_inputs:
  - key: todo.cli
    type: string
    description: "TODO CLI command (e.g. `uv run --project <todo-tool-dir> todo-cli`)"
    default: todo-cli
  - key: todo.validate
    type: string
    description: TODO validate command
    default: todo-validate
  - key: todo.index
    type: string
    description: TODO index command
    default: todo-index
tags:
  - workflow
  - todo
category: workflow
targets:
  claude: true
  codex: true
